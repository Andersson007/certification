---
- name: Test the module
  register: result
  ansible.certification.sample_module:
    name: "ansible-creator" 

- name: Assert the returned message
  ansible.builtin.assert:
    that:
      - result.message == "Hello, ansible-creator"
